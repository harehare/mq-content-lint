# Guide

## Setup

##### Advanced Tuning

This jumps from `h2` straight to `h5`. A tool can't safely auto-fix this: renumbering to
`h3` and inserting two placeholder headings (`h3`, `h4`) are both plausible outcomes, and
only the author knows which section structure was intended.
